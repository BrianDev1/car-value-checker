scalar Date

type User {
  id: Int!
  name: String!
  email: String!
}

input InputCreateUser {
  name: String
  email: String
  password: String
}

# Mutations
type Mutation {
  createUser(inputCreateUser: InputCreateUser!): User!
}
